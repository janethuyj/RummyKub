# Caddy reverse proxy: terminates HTTPS (automatic Let's Encrypt certificate)
# and forwards to the app container. WebSocket upgrades (SignalR) pass through
# automatically — no extra config needed.
#
# DNS: create an A record for rummykub.helleon.com -> your VM's public IP.
rummykub.helleon.com {
    reverse_proxy app:8080
}
